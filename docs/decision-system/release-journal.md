# Release Journal
- status: `current-journal`
- validForCommit: false; Step1186 local CI-fix evidence is green, but the fix is not yet committed and the old pushed SHA remains Windows-red
- supersededBy: null
- currentStep: `1187`
- currentOutcome: `1187:decision-mirror-contract-repaired-and-ci-next`
- latestStepOutcome: `1187:decision-mirror-contract-repaired-and-ci-next`
- authority: docs/decision-system/current-state.md
- fullHistory: ../panel-redesign-decision-log.md

## Current release boundary

- Step1080 preserves the product-owner-approved `192.168.3.5` desktop grammar under one Legacy owner, fixes independent P1 findings without changing the selected eight-tile/32:68 composition, retires stale later-owner gates, regenerates the seven-scenario × two-viewport desktop matrix at 14/14, and passes static engineering readiness. Mobile independent Product/Visual, broader public evidence, clean exact-SHA and remote CL remain open.
- Step1038 selected the line-first Mobile Pulse direction after three materially different phone concepts and prototype-level independent review.
- Step1039 mounted the isolated Mobile Pulse production owner and physically deleted the rejected phone presentation tree.
- Step1040 rejected the first production screenshots despite a green matrix; eight real visual/accessibility defects were returned to implementation.
- Step1041 fixed the production hierarchy, responsive geometry, incident truth context and input/detail defects.
- Step1042 proves the current implementation with 56/56 runtime cells and accessibility-v2 10/10. Exact production Product/Visual review is still pending, so release remains closed.
- Step1043 records independent exact-production Product P1×4 and Visual/Interaction P1×8 VETO. Design remediation is active; matrix evidence cannot open release.
- Step1044 rebuilds the phone task architecture: one normal line instrument, scene-specific incidents, truthful Fleet/all-offline projections, tablet master-detail, single-flow landscape and deduplicated detail.
- Step1045 records the rebuilt 56/56 matrix and accessibility-v2 10/10. Fresh independent Product/Visual review is still open, so release remains closed.
- Step1047 records full Mobile Ops ownership, physical Mobile Pulse deletion and accessibility 10/10 green; exact 56-cell replay, tablet/incident P1 remediation and independent Product/Visual review remain open.
- Step1048 rejects Mobile Ops as continued desktop-narrow grammar, mounts full Mobile Native ownership, physically deletes Mobile Ops, and records current runtime 56/56; accessibility migration and independent Product/Visual resignoff remain open.
- Step1049 records the product owner's explicit veto of Mobile Native as another card-stacked narrow console. Mobile Flow is selected as a clean replacement with an edge-to-edge status rail, one scene-specific instrument, compact object rows and true tablet master/detail. All prior Mobile Native visual/matrix claims are historical for product acceptance.
- Step1056 records the isolated Mobile Flow owner, rejected-owner deletion, 56/56 runtime cells, accessibility-v2 10/10, overview 12/12, independent Visual P0/P1/P2=0 and Product experience with no P1/P2. The Product reviewer correctly refuses release signoff because the exact artifact is still `worktree-*` with release eligibility false; clean committed exact-SHA replay remains mandatory.
- Step1057 creates clean local candidate `62a8358`, migrates stale validators from physically deleted owners to Mobile Flow without weakening truth boundaries, and passes build, overview, package-reference, release-blocker, backend/security/time and dual-surface asset gates. Exact-SHA browser matrices and Product release signoff are now active; GitHub remains untouched.
- Step1058 rejects `48e731c` as a public-release candidate after clean Windows checkout exposed CRLF/LF input-identity drift, normalizes repository text checkouts, regenerates the framework input identity, and proves candidate `6aae922` rebuilds committed public assets with zero diff. Full matrices and Product release signoff remain active; GitHub remains untouched.
- Step1059 migrates public browser/runtime/screenshot probes from physically deleted Mobile Pulse selectors to Mobile Flow, proves canonical mobile route/history behavior, and raises the sole failing landscape resource control from 40px to 44px with rebuilt deterministic assets. Final exact-SHA replay remains active; GitHub remains untouched.
- Step1095 kept the accepted four-screen mobile reference as the sole phone baseline and completed current-owner route validation. Step1096 corrected WAN detail truth/wording and Step1097 fixed fixed-nav occlusion, non-default WAN live-rate reuse, tablet/landscape task structure, exception evidence, object-domain evidence, chart/resource semantics and interface overflow handoff. Current mobile runtime is 56/56, accessibility is 11/11 and route-state is 266/266; overview 28/28 and route-responsive 76/76 must be regenerated after the Step1097 documentation identity change. Engineering readiness remains closed because the Edge 200% artifact is stale for the current identity; no synthetic browser evidence, upload or CL claim is allowed.
- Step1098 records fresh independent Product and Visual PASS with P0/P1=0 after the outage/collection semantics and landscape task-layout fixes. Product/Visual gates are closed; P2 notes remain non-blocking. Engineering release remains closed for stale Edge 200%, dirty worktree, clean exact-SHA and remote CL boundaries.
- Step1110 records the accepted-baseline visual remediation and owner-validator migration. Step1111 then removes temporary inspection helpers and regenerates current-identity evidence: mobile 56/56, accessibility 11/11, overview 28/28, route-responsive 76/76, route-state 266/266 and real Edge toolbar 200% 22/22 are green. Engineering readiness passes in dirty-worktree mode; release remains closed because independent four-role re-signoff, clean exact-SHA and post-upload CL do not exist.
- Step1112 preserves the accepted art direction while adding accessible WAN chart semantics and preventing resource sparkline distortion. The full current-identity engineering evidence remains green; release stays closed for independent signoff, clean exact-SHA and remote CL.
- Step1116 fixes the Edge toolbar UIA fallback by bounding post-menu enumeration to the owned Edge process.
- Step1118 adds typed interface detail evidence, a real resource trend chart and explainable mobile workspace search/filter/sort/pagination without changing the accepted mobile art direction. Current mobile/model/accessibility/public/route evidence is green; the current Edge 200% replay timed out at the bounded cell limit, so the historical 22/22 report is not reused. Independent re-signoff, clean exact-SHA and remote CL remain open.
- Step1129 reduces only the normal WAN chart's vertical footprint (132px to 124px, with proportional compact-phone and landscape overrides) so the existing current-state evidence can enter the 390×844 viewport; accepted colors, typography, radii, navigation grammar and detail/resource chart sizes remain unchanged. Production build, mobile model/architecture checks and bounded 8-cell smoke are green; release remains closed and no `rg.exe` or full browser matrix was run.
- Step1130 restores a task-oriented 600–899px portrait tablet workspace: full-width status, primary WAN object left, route/link plus collection/current-status evidence rail right. The accepted phone grammar, colors, typography and chart semantics remain unchanged; production build and bounded 8-cell smoke are green, while independent review and release remain closed.
- Step1131 keeps the two evidence categories visible at ≤360px by retaining a compact two-column pair with a 6px gap; large-text mode remains one column. Production build and bounded 8-cell smoke are green, and release remains closed.
- Step1132 removes the repeated route/terminal detail summary card while preserving object/source ledgers; resource detail retains its diagnostic summary and trend chart. Production build and focused detail captures are green, and release remains closed.
- Step1133 replaces the repeated exception `采集通道` source row with typed `证据模式`; production build and bounded 8-cell smoke are green, and release remains closed.
- Step1134 confirms the remaining release blocker is current-identity evidence and independent acceptance, not a newly identified Step1133 code failure. Full matrices and Edge replay remain unrun/stale under the CPU-safety boundary; release stays closed.
- Step1135 fixes WAN detail source-route return behavior under the locked mobile art baseline; focused model, architecture, security and type checks pass, while release evidence and independent signoff remain open.
- Step1136 changes the complete mobile matrices and Edge 200% current gates from false-green to pending until exact-identity replay; historical reports remain historical only.
- Step1137 makes the compact mobile evidence time label calendar-aware and keeps timezone-less production values invalid; focused static checks pass and release remains closed.
- Step1138 makes the 56-cell mobile replay incremental and identity-bound while forbidding filtered subsets from setting top-level pass; no browser was launched in this tooling step.
- Step1139 adds the 65% whole-system CPU admission gate and below-normal coordinator priority; the contract passes without launching a browser.
- Step1140 tightens local Windows admission to 55%, requires the one-core below-normal wrapper, and captures current-identity `single × phone320`. The report remains `pass=false`, `complete=false`, `1/56`; 55 cells and all 14 workflows remain, and release stays closed.
- Step1141 appends current-identity `single × phone360` under the same one-core boundary. Admission stays at 42.3% and 43.6%; the report remains top-level fail at 2/56 with 54 cells and all workflows remaining.
- Step1142 appends current-identity `single × phone375` under the same one-core boundary. Admission stays at 39.4% and 43.8%; the report remains top-level fail at 3/56 with 53 cells and all workflows remaining.
- Step1143 refuses the tablet two-column empty-shell false green, adds a real four-destination object patrol workspace with phone isolation and large-text reflow, and resets the identity-bound matrix after rebuilding assets. Fresh tablet and phone cells pass; the current report is 2/56 and release remains closed.
- Step1144 completes the corrected current identity’s eight `single` viewports under the one-core 55% gate. High CPU samples were rejected before capture; the report remains top-level fail at 8/56 with 48 cells and all workflows remaining.
- Step1145 fixes the Fleet mixed-interface truth label from “受影响接口” to “接口状态”, proves the change through red-to-green model/runtime checks and fresh focused captures, invalidates the earlier matrix identity, and switches to focused scenario adjudication before complete replay.
- Step1146 fixes the all-offline scope contradiction from `未运行接口 0 项` to `其他接口未运行 0 项`, proves it red-to-green and keeps focused 390px adjudication active before complete replay.
- Step1147 adds real connection, service-log and read-only-diagnostic recovery destinations to the no-snapshot incident while keeping all untrusted business numbers withdrawn; focused adjudication remains active.
- Step1148 adds `转发状态 / 未测量` to collection failure, explicitly separating management collection, forwarding and business evidence; focused adjudication remains active.
- Step1149 passes focused `resource-full × phone390` visual/evidence adjudication under the one-core CPU gate; no product/style edit was required and the current matrix is 2/56.
- Step1150 passes focused `interfaces-down × phone390`, closes the seven-scenario focused review and moves to serial full responsive replay; current matrix is 3/56.
- Step1151 reaches a one-core scenario-batched 56/56 plus 14/14, then visually vetoes the artifact because incident tablet pages still waste the lower work area.
- Step1152 adds a truthful four-destination task workspace to every incident tablet while leaving phone art unchanged; current identity resets to 1/56 for full replay.
- Step1153 converts interface/Fleet and outage tablets to scene-specific master-detail task rails after a second visual veto of opposite-column whitespace; current identity is 3/56.
- Step1154 closes current mobile engineering evidence at 56/56 cells, 14/14 workflows and all 11 accessibility/200% stages; release remains closed for cross-surface evidence and independent signoff.

- Step1004 proved the now-rejected Origin implementation could pass its engineering and accessibility matrices.
- Step1005 records the product owner's explicit veto of Origin Control as a desktop operations report compressed into a phone. Step1004 product and visual claims are historical.
- Step1008 made Network Pulse the only phone presentation owner and physically deleted Object Cockpit and Origin presentation trees.
- Step1009 records the product owner's veto of Network Pulse after original-image review; its geometry and build evidence is historical and cannot qualify the product.
- Step1010 selected Object Orbit from nine prototype originals, but Step1011 full-surface review rejected its production Overview, Route, Detail, More and Connection as desktop grammar in phone clothing. Its 24/24 geometry is historical engineering evidence only.
- Step1014 switches production to Mobile Vivo, physically deletes Mobile Atom, and records a green 24-cell plus 4-surface runtime capture. Fresh independent review and full gates remain open; release remains closed.
- Step1015 records the owner and three-role veto of Mobile Vivo. Its preview is historical geometry evidence only; a clean Mobile Origin full-surface rebuild is active and release remains closed.
- Step1016 switches production to Mobile Origin, physically deletes Mobile Vivo, and records a green 24-cell plus 4-surface runtime capture. Fresh independent review and complete gates remain open; release remains closed.
- Typed truth, atomic traffic, route identity, read-only safety and dual-surface asset separation remain mandatory foundations, not visual approval.
- GitHub is untouched. The phone direction and worktree engineering/visual acceptance are present, but clean committed current originals, release-eligible Product signoff, complete release gates and exact-SHA remote CL remain absent, so release remains CLOSED. Task is active and `blocked=false`.

## Release rule

Only a clean, independently accepted candidate with complete current-identity matrices, real RouterOS read-only soak, trusted promotion authority and exact-SHA Linux/Windows/GHCR CL may open publication. Normal `git push` is forbidden for this project.
