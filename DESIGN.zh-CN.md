# RouterOS 只读排障面板设计说明

[English](./DESIGN.md) | [简体中文](./DESIGN.zh-CN.md)

本文档用于统一 `ros-ikuai-monitor-panel` 项目的界面设计方向，供后续页面改造、模块新增、样式收敛和 AI 代理执行时参考。

这不是一个通用品牌手册，而是面向本项目落地的设计规则。

## 1. 设计定位

这个面板的视觉和交互气质建议按下面的比例吸收：

- `IBM 70%`
- `HashiCorp 30%`

目标不是做品牌复刻，而是形成一套更适合 RouterOS 只读运维面板的视觉语言：

- 蓝白企业级运维后台
- 冷静、克制、信息密度高
- 更像网络设备控制台，而不是营销官网
- 有爱快式运维节奏，但整体更统一、更干净

## 2. 视觉原则

### 2.1 只读优先

所有页面首先表达的是：

- 当前状态
- 实时上下行
- 累计数据
- 异常与告警
- RouterOS 真正能读到的字段

不要通过视觉暗示让用户误以为这是“可直接改配置”的控制台。

### 2.2 信息密度优先

页面默认要紧凑，而不是为了“显高级”而大面积留白：

- 宁可更密，也不要一屏几乎没内容
- 宁可少装饰，也不要牺牲数据可见行数
- 宁可通过分区和层级降低复杂度，也不要用空白掩盖组织问题

### 2.3 运维阅读顺序优先

网络运维人员通常按这个顺序扫页面：

1. 总览与异常
2. 实时上下行
3. 累计流量与状态
4. 明细字段

因此最重要的实时指标不能被埋在次级信息之后。

### 2.4 模块必须有真实意义

每个模块都要对应真实数据来源，禁止：

- 复制别的模块内容只改标题
- 用重复页面凑功能
- 用没有真实来源的伪指标填空

如果数据拿不到：

- 要么用真实可读字段重构模块
- 要么明确标记“当前未采集”

## 3. 布局规范

### 3.1 壳层结构

默认采用三段式壳层：

- 左侧主导航图标栏
- 左侧当前分组菜单栏
- 顶部系统状态条 + 右侧内容区

这套壳层应尽量稳定，不应被单页功能随意改写。

### 3.2 首页原则

首页只放高价值摘要：

- 核心状态卡
- WAN / 宽带信息
- 实时上下行趋势
- 终端数量
- 系统资源概览
- 快捷入口

首页不适合放：

- 大体量终端明细表
- 大量 ACL / DNS / 路由规则明细
- 全量日志
- 为了“看起来内容很多”而硬堆的低价值模块

### 3.3 二级页原则

详细数据应下沉到对应监控页。每个二级页至少要有：

- 自己的摘要区
- 自己的主表或主列表
- 自己的细节区或趋势区
- 内容与菜单命名严格对应

## 4. 颜色与状态

建议保留当前蓝白主体系：

### 主色

- Primary：`#4794EB`
- Primary Deep：`#0C53BD`
- Primary Soft：`#E4EFFC`
- Primary Soft 2：`#F0F6FD`

### 背景

- Page：`#F6F8FB`
- Sidebar：`#F8FBFF`
- Card：`#FFFFFF`
- Card Alt：`#FCFDFF`

### 文本

- Main Text：`#333333`
- Soft Text：`#666666`
- Dim Text：`#999999`

### 状态色

- 正常：`#24C271`
- 预警：`#FF9800`
- 异常：`#F53F3F`
- 信息：`#4794EB`

## 5. 图表与表格

### 5.1 图表

- 图表必须有图例或明确标题
- 如果是百分比趋势，优先提供 `0 / 50 / 100%` 坐标标识
- 如果是上下行趋势，必须明确谁是上行、谁是下行
- 图表应服务于判断，不是纯装饰

### 5.2 表格

- 优先高密度、可扫读
- 允许交替行背景以增强长表可读性
- 避免过重阴影和过大的单元格内边距
- 标题行固定时，必须避免遮挡正文

## 6. 交互与滚动

- 页面滚动要连续、可预期，不能抢用户鼠标控制权
- 吸顶模块只能辅助阅读，不能遮挡正文
- 左侧导航和内容区域的固定逻辑必须一致，不应出现某些页面固定、某些页面漂移

## 7. 适配要求

- 宽屏桌面是主场景
- 平板需要单独优化，尤其是横屏大屏 WebView 容器
- 不要用“响应式降级”为借口把信息砍得过少

## 8. 最终判断标准

一个页面是否合格，可以用这几个问题快速判断：

1. 一眼能不能看到最重要的实时状态？
2. 有没有明显的无意义留白？
3. 有没有重复模块或伪信息？
4. 图表有没有实际可读意义？
5. 这个页面更像运维工具，还是更像空洞展示页？

如果答案偏向后者，就说明还需要继续收敛设计。
